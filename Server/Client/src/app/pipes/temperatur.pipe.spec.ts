import { TemperaturPipe } from './temperatur.pipe';

describe('TemperaturPipe', () => {
  it('create an instance', () => {
    const pipe = new TemperaturPipe();
    expect(pipe).toBeTruthy();
  });
});
